# RFP Intake (Brasaland) — Part 1

Upload UI lives in the **existing** backoffice (`uis/backoffice/rfp-upload.html`), not a new frontend app.

Conversion: `markitdown`. Readability: `py-readability-metrics`. API: `services/rfp`.
