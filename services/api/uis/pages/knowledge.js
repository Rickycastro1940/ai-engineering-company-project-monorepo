/**
 * The knowledge assistant UI lives in uis/knowledge/ (index.html + chat.js).
 * Chat turns use WS /knowledge/ws.
 */
export default function KnowledgeBaseQueryUI() {
  if (typeof window !== "undefined") {
    window.location.replace("/knowledge/");
  }
  return null;
}
export default function KnowledgeBaseQueryUI() {
  if (typeof window !== "undefined") {
    window.location.replace("/knowledge/");
  }
  return null;
}
