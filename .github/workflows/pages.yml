# GitHub Pages environment allows `main` and `feature/websocket-chat`.
# A deploy from any other branch is rejected by environment protection rules.

on:
  push:
    branches:
      - feature/websocket-chat
      - main
    paths:
      - "uis/website/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: uis/website
      - id: deployment
        uses: actions/deploy-pages@v4
