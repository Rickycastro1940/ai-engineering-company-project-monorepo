fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pandas>=2.2.0
openai>=1.0.0
python-dotenv>=1.0.0

# Dependencies are managed with uv. Install/sync with:
#   uv sync
# See pyproject.toml for the canonical dependency list.
